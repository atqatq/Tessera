[package]
name = "tessera-access"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Pure five-layer permission engine for the Tessera kernel: deny wins, default deny, no I/O."
[lints]
workspace = true

[dependencies]
tessera-ids = { path = "../ids" }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
