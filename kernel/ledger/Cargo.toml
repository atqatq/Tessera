[package]
name = "tessera-ledger"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Per-tenant append-only hash chains for the Tessera kernel: SHA-256 via an audited crate, never hand-rolled."
[lints]
workspace = true

[dependencies]
tessera-ids = { path = "../ids", version = "0.1.0" }
sha2.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
serde_json.workspace = true
