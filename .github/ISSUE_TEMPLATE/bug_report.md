---
name: Bug report
about: Something behaves contrary to spec
labels: ["bug"]
---

**Component** — kernel crate (`tessera-ids` / `tessera-access` / `tessera-ledger` / `tessera-inv`), the Python reference, or documentation:

**What happened**:

**What the spec/reference says** (cite the doc, ADR, or vector):

**The failing test** — a test that reproduces the bug and fails today. Paste it here; a PR with only that test (red) is a complete, mergeable contribution on its own. A conformance vector works too — see [the how-to](../site/src/how-to/run-vectors.md):

```
// failing test or vector id
```

**Environment** (commit, platform, toolchain version):
