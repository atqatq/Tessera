# Pinned for reproducible builds. MSRV is 1.85 (declared in
# [workspace.package].rust-version and exercised by a dedicated CI job);
# the pinned toolchain is simply the current stable the maintainers use.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
